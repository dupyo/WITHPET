<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- JAVA와 연결할 Mapper 파일 설정 -->
<mapper namespace="poly.persistance.mapper.CenterMapper">
	<!-- 게시물 작성 -->
	<insert id="insertCenterProc" parameterType="CenterDTO">
		
		INSERT INTO WIP_BOARD
			(
				USER_NO,
				BOARD_TITLE,
				BOARD_CONTENT,
				BOARD_LEVEL,
				REG_DATE,
				REG_NO
				
			)
			VALUES
			(
				#{userNo},
				#{title},
				#{content},
				1,
				NOW(),
				#{userNo}
			)
			
	</insert>
	
	<!-- 리뷰 리스트 불러오기 -->
	<select id="getCenterList" resultType="CenterDTO">
		
		SELECT 
			R.REVIEW_NO as reviewNo,
			R.USER_NO as userNo,
			U.USER_NAME as userName,
			R.BOARD_NO as boardNo,
			R.REVIEW_CONTENT as content,
			R.REG_DATE as regDate,
			R.UPD_DATE as updDate
		FROM BOARD_REVIEW R
		INNER JOIN USER_INFO U
		ON R.USER_NO=U.USER_NO
		WHERE B.BOARD_NO=#{boardNo}
		
	</select>
	
	<!-- 게시물 상세 불러오기 -->
	<select id="getCenterDetail" parameterType="string" resultType="CenterDTO">
		
		SELECT 
			B.BOARD_NO as boardNo,
			B.USER_NO as userNo,
			U.USER_NAME as userName,
			B.BOARD_TITLE as title,
			B.BOARD_CONTENT as content,
			B.BOARD_COUNT as count,
			B.REG_DATE as regDate,
			B.UPD_DATE as updDate,
			B.BOARD_LEVEL as boardLevel
		FROM WIP_BOARD B
		INNER JOIN USER_INFO U
		ON B.USER_NO=U.USER_NO
		WHERE B.BOARD_NO=#{boardNo}
		
	</select>
	
	<!-- 게시물 삭제 -->
	<delete id="deleteCenterProc" parameterType="string">
	
        DELETE 
        FROM WIP_BOARD
		WHERE BOARD_NO=#{boardNo}
	
	</delete>
	

</mapper>